<!-- Mini Project: Online Shopping Cart with Custom Exception
Scenario

User adds items to the shopping cart.

If the item is out of stock, throw a OutOfStockException.

If the user tries to checkout with empty cart, throw a EmptyCartException.

Handle exceptions gracefully.






GENERICS :

Generic Class

Create a generic class Box<T> that stores one value of any type.

Add methods SetValue(T value) and GetValue().

Test it with int, string, and double.

Generic Method

Write a generic method Swap<T>(ref T a, ref T b) that swaps two values.

Call it with integers, strings, and doubles.

Generic List Wrapper

Create a class MyList<T> that internally uses a List<T>. -->